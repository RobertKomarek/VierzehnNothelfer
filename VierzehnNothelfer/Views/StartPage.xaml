<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:VierzehnNothelfer.ViewModels"
    xmlns:model="clr-namespace:VierzehnNothelferBackup.Models"
    x:Class="VierzehnNothelfer.Views.StartPage">

    <ContentPage.BindingContext>
        <vm:NothelferViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <CollectionView ItemsSource="{Binding ListeNothilfen}"
                            ItemsLayout="VerticalGrid, 2">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="A" BackgroundColor="Red" VerticalTextAlignment="Center" />
                                <Label Text="{Binding .}" FontSize="Large" VerticalTextAlignment="Center" />
                            </StackLayout>
                            <BoxView HeightRequest="0.7" Color="LightGray"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

            </CollectionView>
            <Button Text="Zu den Nothelfern" Clicked="Button_Clicked" Margin="10,0,10,10" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
